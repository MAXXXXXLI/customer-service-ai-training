# 项目发布规则

- 本项目唯一的正式访问地址是 `https://maxxxxxli.github.io/customer-service-ai-training/`。
- 每次发布都从 `main` 分支覆盖同一个 GitHub Pages 站点；不得为新版本创建新的公开站点、预览站点、发布分支或版本网址，除非用户明确要求。
- 对用户提供网站地址时，始终使用上面的固定地址，不要添加 `?v=...`、提交号或其他版本参数。
- 旧的 GitHub Actions 运行记录仅作为历史保留，不代表仍有旧网站在线；不得为了清理历史而关闭当前 Pages 环境或部署工作流。
- 前端静态资源可以使用内部缓存版本参数，但这些参数不得出现在提供给用户的正式访问地址中。
- 发布完成后，确认远程 `main`、最新成功的 Pages 工作流和线上页面均对应本次提交。
